stages:
  - test
  - build

variables:
  DOCKER_TLS_CERTDIR: ""

test:
  stage: test
  image: node:22-alpine
  script:
    - npm test
    - npm run analyze -- --check

validate-compose:
  stage: test
  image: docker:27
  script:
    - docker compose -f deploy/compose.local.yml config --quiet
    - docker compose -f deploy/portainer-stack.yml config --quiet

.docker-build:
  stage: build
  image: docker:27
  services:
    - name: docker:27-dind
      alias: docker
  before_script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

build-app:
  extends: .docker-build
  script:
    - docker build -t "$CI_REGISTRY_IMAGE/app:$CI_COMMIT_SHORT_SHA" -t "$CI_REGISTRY_IMAGE/app:latest" .
    - docker push "$CI_REGISTRY_IMAGE/app:$CI_COMMIT_SHORT_SHA"
    - docker push "$CI_REGISTRY_IMAGE/app:latest"

build-brouter:
  extends: .docker-build
  script:
    - docker build -f Dockerfile.brouter -t "$CI_REGISTRY_IMAGE/brouter:$CI_COMMIT_SHORT_SHA" -t "$CI_REGISTRY_IMAGE/brouter:latest" .
    - docker push "$CI_REGISTRY_IMAGE/brouter:$CI_COMMIT_SHORT_SHA"
    - docker push "$CI_REGISTRY_IMAGE/brouter:latest"
